# AI 에이전트 공통 페어 프로그래밍 가이드라인 & 규칙 (AGENTS.md)

OpenRouter API 연동 기반 AI 프롬프트 평가 기능을 갖춘 로컬 프롬프트 보관함 웹앱(Express 백엔드 + 정적 `index.html` 프론트엔드)입니다.

---

## 1. 보안 가이드라인 (Security)

- **기본 원칙: `.env` 및 민감 정보/비밀 키 파일의 내용을 절대 출력창(터미널/도구 로그)에 출력하지 않습니다.** (`credentials*`, `*.pem`, `*_key*`, `id_rsa*` 등)
  - `cat`, `type`, `Get-Content`, 읽기 도구, 파이프라인 전달 등 어떠한 방식으로도 내용을 출력해서는 안 됩니다. ("로컬 환경이라 괜찮다", ".gitignore에 등록되어 있다", "설정만 재확인하겠다" 등의 사유를 불문하고 적용됩니다. `.gitignore`는 커밋을 방지할 뿐, 도구가 읽어 출력창/로그에 노출되는 것을 막아주지 못합니다.)
  - 파일의 존재 여부 확인(`ls`/`Glob`/`Test-Path`)이나 특정 키 이름의 존재 여부 확인(매칭된 실제 값을 출력하지 않는 방식)은 허용됩니다.
  - **유일한 예외**: 사용자가 특정 인증 정보의 실제 값을 명시적으로 확인/디버깅해달라고 요청한 경우(예: "내 API 키가 왜 거부되는지 .env 파일 내용을 출력해줘")에만 허용됩니다. 이러한 명시적 요청이 없는 한, 단순 설정 확인 목적이라 하더라도 비밀 키가 담긴 파일을 열지 않습니다.
  - 작업 수행에 비밀 값 조회가 꼭 필요한지 불명확한 경우, 임의로 읽지 말고 사용자에게 먼저 질문합니다.
- 현재 `.env` 파일에 `OPENROUTER_API_KEY`가 보관되어 있습니다.
- 만약 실수로 비밀 정보가 출력창에 노출된 경우, 즉시 사용자에게 알리고 해당 인증 정보를 즉각 재발급(Rotate)하도록 안내합니다. (예: https://openrouter.ai/settings/keys 에서 OpenRouter 키 재발급)

---

## 2. 질문/브레인스토밍과 실제 작업(코드 수정)의 엄격한 분리

- **질문/기획/브레인스토밍 요청 시**:
  - 사용자가 기획, 아이디어, 구조 개선, 동작 원리 등에 대해 질문하거나 의견을 물을 때는 **설명과 답변, 방안 제안만** 수행합니다.
  - 임의로 소스 코드를 수정하거나, 파일을 생성/삭제하거나, 프로그램을 조작하지 않습니다.
- **실제 작업(코드 변경/실행) 수행 전 필수 확인 절차**:
  - 단순 답변을 원하는지 실제 코드 수정을 원하는지 불분명한 경우, 또는 논의 후 실제 구현에 들어갈 때는 **"이해한 작업 내용 및 변경 계획"을 먼저 명확히 안내하고 사용자의 확인/허락을 받은 후** 실행합니다.

---

## 3. 파일 접근 및 경로 탐색 권한 원칙

- 기본 워크스페이스(프로젝트 루트 디렉터리) 외부의 새로운 디렉터리나 시스템 경로를 탐색해야 할 경우, 반드시 사용자에게 목적을 설명하고 사전 허락을 받습니다.

---

## 4. 안정성 및 점진적 변경 원칙

- 사용자의 확인 없이 기존의 핵심 기능이나 레이아웃 구조를 임의로 크게 변경하지 않습니다.
- 모든 기능 수정은 변경 전후 영향도를 고려하여 안전하게 진행합니다.

## 5. Git 제어 권한 원칙 (자동 Git 조작 엄격 금지)

- `git add`, `git commit`, `git push`, `git checkout` 등 일체의 Git 명령어는 **사용자가 명시적으로 직접 지시하지 않는 한 절대 자동으로 실행하지 않습니다.**
- 모든 Git 커밋 및 버전 관리는 사용자가 직접 터미널에서 수동으로 제어합니다.

---

## 6. 작업 실패 시 규칙 (Failure Handling)

- 같은 오류가 **2회 연속** 발생하면 같은 방식으로 3번째 시도하지 않습니다.
  대신 즉시 멈추고, 다음 3가지를 사용자에게 보고합니다:
  1) 무엇을 시도했는지
  2) 왜 실패했다고 추정하는지 (에러 로그 근거)
  3) 가능한 대안 2가지 이상 (장단점 포함)
- "아마 될 것 같다", "이번엔 다를 것 같다" 같은 근거 없는 재시도를 금지합니다.
  이전 시도와 다른 접근인지 스스로 확인한 후에만 재시도합니다.
- 원인 파악이 안 된 채로 코드를 대량으로 고치지 않습니다.
  먼저 원인을 1~2문장으로 설명하고 승인받은 뒤 수정합니다.

---

## 7. 판단 범위 제한 (Scope Guardrail)

- 요청받지 않은 파일/모듈은 건드리지 않습니다. 수정이 필요해 보이면 먼저 물어봅니다.
- 라이브러리/패키지를 새로 설치하기 전에 반드시 사용자에게 알리고 허락을 받습니다.
- 데이터베이스 스키마 변경, 파일 삭제, git force-push 등 되돌리기 어려운 작업은 반드시 실행 전 확인을 받습니다.

---

## 8. 완료 보고 기준 (Evidence-based Completion)

- "됐다", "작동할 것이다"라는 주관적 추측 대신, 실제 실행 결과(테스트 통과/빌드 성공 로그/정확한 검증 결과)를 보여준 뒤에만 완료로 간주합니다.
- 스스로 "일단 이 정도면 됐다"고 범위를 임의 축소해서 넘어가지 않습니다. 안 되는 부분이 있다면 명확하게 사실대로 보고합니다.

---

## 9. 토큰 및 시간 절약 원칙

- 탐색(파일 읽기)이 예상보다 많이 필요하면, 전체를 무분별하게 읽지 않고 관련 디렉터리 및 핵심 파일로 범위를 좁혀서 탐색합니다.
- 큰 작업은 한 번에 모두 진행하지 않고, 논리적 단계별로 나누어 각 단계마다 결과를 보고하고 사용자의 확인 후 다음 단계로 진행합니다.


